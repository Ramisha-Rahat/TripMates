import 'package:flutter/material.dart';
class Alertscreen extends StatelessWidget {
  const Alertscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Container(
        child: Center(
          child: Text('alerttttttttttttt'),
        ),
      )
    );
  }
}
